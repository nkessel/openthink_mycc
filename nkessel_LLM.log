2026-09-25 — Claude (claude-opus-5-5), working for Nathan (nkessel)
Branch: claude/forms-org-import-fab → PR into development_branch

What / why
- Created development_branch from main (it didn't exist yet; guideline 10 says we work there). No content changes on it; everything else goes through this PR (guideline 9).
- git pull + checked open PRs first (none) (guideline 8).
- Org data: added scripts/import-orgs.mjs and ran it against aissatabarry/ma-climate-coalition-map
  (commit e8bb5ce, 2026-09-25) because Nathan said that repo is the most up-to-date org source.
  -> 150 orgs, 88 logos (public/logos/*.png, extracted from embedded base64), 175 memberships.
  -> Coalitions now match the source's 8: MPF, MYCC, EJLT, GJC, CRS, HERO kept their projects/events/actions;
     CCCC and SCJC added (no projects/events yet).
  -> Dropped mara, gfn, ghs, zcrf (not in the source) with their 12 projects, 12 events, 12 actions,
     per "get rid of any projects or events that belong to orgs not on this list". Projects/events are
     attached to coalitions in our schema, so "belongs to" was read as "belongs to a coalition not on the list".
  -> Source has no member ties for CCCC, CRS, HERO, so they show 0 members.
  -> Source has no town/description for orgs, and type only for school clubs; the rest are "unknown"
     until groups fill them in via the org form.
- Forms + sheet: apps-script/Code.gs builds 4 Google Forms (org, event, project, feedback) linked to a sheet,
  seeds the sheet from development_branch's data.json, applies submissions (blank = keep), logs changes,
  and serves public data as JSON. All questions optional except "which one is this about?" (and the feedback
  message), per Nathan's request. Includes every question from the team's existing "Climate Map Modification
  Form" (organization, contact info, event name/description, date/time/location, climate topic tags).
  Tested with a mocked Sheets/Forms runtime in Node (seed → data.json round-trip, and simulated submissions).
  Not yet run inside real Google Apps Script — first real run may surface permission/API quirks.
- App: + floating action button (src/fab.ts) with the 4 forms, pre-filled with the org/coalition open in the
  drawer; org drawer shows logo, abbreviation, profile scores, and an "Update this organization's info" link.
  Form URLs go in src/forms.config.ts after setup (empty until then; menu says "Form not connected yet").
- Added LLM_GUIDELINES.md (featured in README) and CLAUDE.md so Claude Code sessions pick the rules up.

2026-09-25 (later) — Claude, same PR (#1, not yet merged)
- Nathan: events/projects can belong to coalitions OR orgs. Changed the schema: org-owned items now live in
  organizations[].projects/events; coalition items stay under coalitions (optionally with host_org_id).
  Events/Projects views and the org drawer (new Projects + Events tabs) show both. Forms explain the choice;
  items with neither are kept in the sheet but flagged in Change Log and left off the map.
  Replaces my earlier workaround (org-only items were filed under the org's first coalition).
- To raise confidence before the first real Google run: added `npm run check:apps-script` (type-checks Code.gs
  against @types/google-apps-script — catches misspelled/nonexistent Google API calls) and
  `npm run test:apps-script` (the Node simulation tests, now in the repo), plus a GitHub Actions CI workflow
  running build + both checks on every PR. Wrapped Form.shortenFormUrl in try/catch (legacy API; falls back to the long link).

2026-09-25 (afternoon) — Claude, same PR (#1), per Nathan's requirements review
- Hosting: stays static on GitHub Pages (relative base, Pages deploy workflow from development_branch). Site reads the
  sheet's public JSON live (src/data.config.ts LIVE_DATA_URL) and falls back to public/data.json; nightly Action opens a
  snapshot PR when data changed. Couldn't enable Pages via API from here — repo owner does 2 clicks (SETUP.md step 5).
- Access control (Nathan's design, replacing per-org passwords): forms require Google sign-in and record the verified
  email; Editors tab maps emails → org_ids / coalition_ids; admins = Nathan, Aissata, Turibius. Unauthorized, no-email,
  and new-org submissions go to "Needs Review" (tick approve), are flagged in Change Log, and email the admins.
  Every submission (incl. feedback and refused approvals) gets a Change Log row. Approved new orgs make their proposer
  the point-person.
- Org-to-org connections: org form page "Who you work with" (weekly / monthly / a few times a year / yearly or less);
  stored in Connections tab; data.json org_links (one per pair, strongest answer wins, both reporters kept); drawn as
  weighted amber edges on the graph with a toggle.
- Locations: events and projects get location + "online?" (geocoded on submit); geographic view has Events/Projects
  checkboxes (off by default). Orgs get HQ address + "remote" (no pin).
- Search: graph search box, Organizations tab (search + filters), geographic search (places via OSM Nominatim,
  orgs, coalitions → fit to members, events, projects) and "What's near me?".
- Holes: src/suggestions.ts scores unconnected org pairs (TF-IDF of name/description/tags, shared tags/skills, type,
  youth-serving, distance); shown per org (drawer "Suggested") and as a list. Same-name pairs are flagged as possible
  duplicates (the source data has e.g. 3× Citizens Climate Lobby).
- Also: orgs with no coalition were hidden on the graph (pre-existing bug) — now shown.
- Verified: build, Apps Script type check against official typings, Node simulation tests (access rules, review
  approval, change log count, connections, locations, round trips), screenshots with injected test data.
  Not verified: a real run in Google Apps Script (setEmailCollectionType/setPublished are feature-detected with fallbacks).
- Shared the "Openthink data" sheet with ab130@wellesley.edu and turibius@bu.edu as editors (Drive).

2026-09-25 14:35 — Claude
- At Nathan's request: merged PR #1 into development_branch, renamed the Google Sheet to "MA Climate Coalition Map
  data" (Drive), and updated the name in Code.gs/SETUP.md (this PR). The Apps Script paste + first run must be done by
  Nathan in his Google account — no tool here can run Apps Script or reach his browser.

2026-09-25 15:30 — Claude
- Setup done in Google/GitHub (no code): pasted Code.gs into the sheet's bound Apps Script (via Nathan's Chrome — the
  in-app browser pane can't open the Apps Script popup) and ran Set up sheet + forms (Nathan approved the permissions):
  150 orgs loaded, 4 forms built. GitHub Pages turned on (Source: GitHub Actions; development_branch allowed in the
  github-pages environment); deploy re-run succeeded, site live at https://nkessel.github.io/openthink_mycc/.
- Leftover: a standalone "Untitled project" in Nathan's Apps Script (my first attempt, not bound to the sheet) — safe to delete.
- This PR, per Nathan ("replace Openthink with MA Climate Coalition Map", scope = what people see): site title/header,
  sheet menu, form titles/descriptions, admin alert subject, docs. Kept internal names (repo, package name,
  OPENTHINK_DATA_URL, localStorage key) so nothing breaks. Re-running "Set up sheet + forms" now also retitles the
  already-built forms, since those were created with the old name.

2026-09-25 15:35 — Claude (connect PR, stacked on the rename PR)
- Pasted the Start Here "For the site" JSON into src/forms.config.ts, so the + button and "Update this info" links open
  the real forms (pre-filled with the open org/coalition). LIVE_DATA_URL still empty: the web app deployment errored when
  I tried it from Chrome; waiting on Nathan to deploy it (SETUP.md step 3) and send the /exec URL.
- 16:20 — Web app deployed (Anyone, execute as Nathan; Nathan approved the authorization). Checked it: returns 150 orgs /
  8 coalitions, no emails. Added it as LIVE_DATA_URL, so approved form edits show up on the site without code changes.

2026-09-25 16:30 — Claude (launch wrap-up)
- Nathan said to do everything: merged PRs #3 (rename) and #4 (forms + live data) after CI passed; Pages redeployed.
  Verified on the live site: title/header "MA Climate Coalition Map", data loads from the sheet's web app.
- Pasted the renamed Code.gs into the sheet's Apps Script and re-ran setup (Nathan approved Google's re-authorization):
  sheet menu and all four forms now say "MA Climate Coalition Map"; also renamed the forms' Drive files to match.
- Added repo variable OPENTHINK_DATA_URL (= web app URL), which turns on the nightly snapshot PRs (sync-data.yml).
- 16:35 — Fixed the Start Here intro line (A3) to say "MA Climate Coalition Map"; moved my stray standalone
  "Untitled project" Apps Script to Drive trash (recoverable).
- Not done: an end-to-end test submission (blocked as a write to live shared data) — Nathan to submit one test event.

2026-09-25 19:15 — Claude
- Nathan: the "[org_id]" in form dropdowns looks weird; make names unique instead (e.g. the 3 "Citizens Climate Lobby"
  rows get their chapter in the name, like the 350 Mass groups). Done: dropdowns now show plain names
  (coalitions as "Name (ABBR)", events as "Name — date"). The script maps a picked name back to its row by matching
  the sheet as it is at submit time; " [id]" is added only if two records of the same kind still share a name, so a
  duplicate can never silently hit the wrong row. Old "[id]" labels (e.g. queued Needs Review items) still work.
  Site pre-fill (src/fab.ts) uses the same rule. Tests: plain names, duplicate-name fallback, org rename then re-pick.
- The sheet's id column stays: Connections, Editors, Events/Projects point to orgs by id, and ids never change.
- Renamed in the sheet: citizens_climate_lobby_boston → "Citizens Climate Lobby Boston",
  citizens_climate_lobby_south_shore_cape_and_islands → "Citizens Climate Lobby South Shore, Cape and Islands".

2026-09-25 19:25 — Claude
- Nathan: replace the four "who you work with" checkbox lists with one list of orgs, checkboxes per frequency, blank =
  no active contact. Now a checkbox grid (rows = orgs, columns = Weekly / Monthly / A few times a year / Yearly or less).
  If a row has several ticks, the most frequent wins. An entirely blank grid keeps the org's current answers, so an org
  that only edits its description doesn't lose its connections (judgment call — flag if Nathan wants blank = clear all).
  Re-running "Set up sheet + forms" swaps the old questions for the grid on the live form; old-style answers still parse.
